# Leave empty for local dev (Vite proxy handles /api)
# Set to your Render backend URL for production
VITE_API_URL=
VITE_BACKEND_URL=http://localhost:5001
VITE_STRIPE_PUBLIC_KEY=

# Vercel serverless proxy target. Required in production.
BACKEND_URL=
